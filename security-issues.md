| ID | Severity | Issue | File Path | Line Number(s) | Recommendation |
| --- | --- | --- | --- | --- | --- |
| 1 | Critical | CORS same-origin checks trust request-supplied `x-forwarded-host` and `x-forwarded-proto`, allowing a browser to spoof the computed origin with custom headers and receive `Access-Control-Allow-Origin` for an attacker-controlled origin. This can expose same-origin Custom endpoints and Dev inspection routes cross-site. | [src/server-runtime-source.ts](src/server-runtime-source.ts) | 498-504 | For Hosted Capsules, compare `Origin` against the configured Hosted Capsule public origin such as `https://<subname>.<hosted-domain>` instead of deriving authority from arbitrary forwarded headers. If forwarded headers are used, accept them only when Caddy overwrites them and they match the configured hosted domain/origin. Keep local Dev localhost handling separate, and add regression tests proving attacker-supplied `X-Forwarded-*` headers cannot satisfy same-origin checks. |
| 2 | Critical | Dev inspection endpoints expose logs, database table lists, database dumps, arbitrary SQL query execution, and local identity simulation without an authentication secret. Combined with the CORS bypass above, a malicious webpage could read or mutate local Dev session state. | [src/cli/sporades.ts](src/cli/sporades.ts) | 1390-1459, 1463-1479 | Generate an unguessable per-session inspection token when the Dev session starts, store it in `.sporades/dev-session.json`, and require it on every `__sporades/debug/*` request through a dedicated header such as `x-sporades-inspection-token`. The CLI should read and send the token automatically; browser requests without the token should receive `404` or `403`. |
| 3 | High | `runReadOnlyInspectionQuery` is not actually read-only: it prepares attacker/user-supplied SQL and calls `statement.all()` without enforcing read-only opcodes. Node SQLite executes side-effect statements such as `CREATE`, `INSERT`, `ATTACH`, and mutating `PRAGMA` statements through this path; the CLI allowlist also permits any `PRAGMA`. | [src/server-runtime-source.ts](src/server-runtime-source.ts)<br>[src/cli/sporades.ts](src/cli/sporades.ts) | 1399-1413<br>3551-3552 | Set live Dev inspection connections to `PRAGMA query_only = ON`, use a SQLite authorizer or a strict parser that permits only `SELECT` and safe metadata PRAGMAs, reject multi-statement input, and keep the direct-container read-only safeguards for every inspection path. |
| 4 | High | WebSocket upgrade handlers check only the pathname and do not validate the `Origin` header before accepting the client transport. Browser CORS does not protect WebSockets, so cross-site pages can open a Sporades transport and invoke anonymous-session queries, mutations, auth flows, file APIs, and app messages. | [src/cli/sporades.ts](src/cli/sporades.ts)<br>[src/templates/server-bundle-template.ts](src/templates/server-bundle-template.ts)<br>[src/server-runtime-source.ts](src/server-runtime-source.ts) | 1512-1518<br>97-103<br>6403-6425 | Apply the same origin intent as HTTP CORS before sending `101 Switching Protocols`: for Hosted Capsules, accept only `Origin` values matching the configured public origin such as `https://<subname>.<hosted-domain>`, with Caddy-overwritten forwarded headers used only when they match that configured hosted authority. Keep local Dev localhost/Public Dev handling separate, and add tests for cross-origin WebSocket rejection. |
| 5 | High | Session tokens are transported in WebSocket query strings and Custom endpoints also accept `sessionToken` from the query string. Query tokens are commonly captured by access logs, browser tooling, reverse proxies, crash reports, and copied URLs. | [src/templates/client-runtime-template.ts](src/templates/client-runtime-template.ts)<br>[src/server-runtime-source.ts](src/server-runtime-source.ts) | 166-172<br>5372-5373, 6421-6425 | Remove query-string session-token transport. Authenticate WebSockets with an initial auth message, a short-lived one-time connection nonce, or a vetted subprotocol flow, and accept endpoint session tokens only through headers. |
| 6 | High | Uploaded files are served from the Capsule origin using the client-provided MIME type, and public URLs can expose those bytes under `__sporades/files/public/*`. A user-controlled `text/html` or SVG payload can become same-origin stored XSS, especially because the default CSP is report-only and permits inline scripts. | [src/server-runtime-source.ts](src/server-runtime-source.ts)<br>[src/cli/project-config.ts](src/cli/project-config.ts) | 4604-4619, 4691-4692, 4873-4877<br>11-14, 142-144 | Serve uploaded bytes only with a safe MIME allowlist. Preserve known safe inline types such as common raster images and plain text as appropriate, and rewrite every non-allowlisted or ambiguous MIME type to `application/octet-stream` before sending private or public file URL responses. Add regression tests for `text/html`, SVG, XML, and missing/unknown types. |
| 7 | Medium | Email sign-in has no visible rate limiting, lockout, or per-account throttling. An attacker can repeatedly send `auth.signIn` attempts over the WebSocket transport and brute-force weak passwords. | [src/server-runtime-source.ts](src/server-runtime-source.ts) | 6613-6628, 7170-7188 | Add per-IP, per-email, and per-session throttling with exponential backoff and audit events. Consider temporary account lockouts or proof-of-work/CAPTCHA hooks for repeated failures in hosted environments. |
| 8 | Medium | Custom endpoint and debug JSON request bodies are read fully into memory without a size cap before parsing. Large request bodies can exhaust memory or stall the runtime. | [src/server-runtime-source.ts](src/server-runtime-source.ts) | 371-377, 6041-6046 | Enforce a global request-body limit for JSON and Custom endpoints, stream with an accumulating byte counter, reject oversized bodies with `413 Payload Too Large`, and make the limit configurable. |
| 9 | Low | Unhandled HTTP errors are returned to clients as raw error messages. This can leak internal paths, adapter details, SQL errors, or operational context. | [src/cli/sporades.ts](src/cli/sporades.ts)<br>[src/templates/server-bundle-template.ts](src/templates/server-bundle-template.ts) | 1506-1509<br>91-93 | Return a generic `500 Internal Server Error` body to clients and log detailed errors server-side with redaction. Keep structured, non-secret diagnostics behind authenticated inspection surfaces. |

No additional issues were included beyond the top 10. Dependency audit note: `npm audit --json` reported 0 known vulnerabilities across the installed dependency graph at audit time.
