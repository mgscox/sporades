# 04 — Run Preact Capsules through Vite

**What to build:** Preact Capsule authors can select either esbuild or Vite and receive equivalent scaffolds, Sporades SDK behavior, normalized release output, and Dev-session feedback.

**Blocked by:** 03 — Add the Vite client toolchain through React.

**Status:** ready-for-agent

- [ ] Project configuration and scaffolding accept both supported Preact/toolchain combinations and reject unsupported values with structured guidance.
- [ ] Preact/Vite uses the correct JSX and refresh integration without pulling React into the client output.
- [ ] Existing Preact templates build and behave equivalently through esbuild and Vite.
- [ ] Preact/Vite Capsules run through Dev, Container, and Hosted execution with structured rebuild recovery and release parity.
- [ ] Documentation describes how existing Preact Capsules retain esbuild and how authors opt into Vite.
