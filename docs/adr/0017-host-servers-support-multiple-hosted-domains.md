# Host servers support multiple Hosted domains

A single Host server installation can manage multiple Hosted domains from the outset. Host profiles keep the CLI simple by selecting one SSH target and one Hosted domain, while the Host server registry, storage layout, container naming, and reverse-proxy generation remain domain-scoped so adding another domain to the same machine does not require a registry migration later.
